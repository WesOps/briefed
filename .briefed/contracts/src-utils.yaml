module: src/utils
files: 3
complexity: 0
exports:
  - StackInfo
  - 'detectStack(root: string): StackInfo'
  - 'extToLanguage(ext: string): string | null'
  - PARSEABLE_EXTENSIONS
  - SKIP_DIRS
  - GraphNode
  - 'computeRefCounts(nodes: Map<string, GraphNode>): Map<string, number>'
  - 'countTokens(text: string): number'
  - 'formatTokens(count: number): string'
  - 'formatBytes(bytes: number): string'
dependents:
  - src-commands
  - src-extract
  - src-generate
