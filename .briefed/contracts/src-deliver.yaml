module: src/deliver
files: 5
complexity: 0
exports:
  - 'generateGitHubAction(root: string)'
  - 'updateClaudeMd(root: string, skeleton: string)'
  - 'saveSkeletonFile(root: string, skeleton: string)'
  - 'writeCursorRules(root: string, skeleton: string, conventions: string)'
  - 'writeAgentsMd(root: string, skeleton: string, conventions: string)'
  - 'installGitHook(root: string)'
  - 'removeGitHook(root: string)'
  - 'installHooks(root: string)'
  - 'generateHookScripts(root: string)'
dependents:
  - src-commands
