# Project Context (generated by briefed)
# This file provides Cursor with structural context about this codebase.
# Regenerate with: npx briefed init

# briefed: typescript, javascript project
Stack: typescript, javascript
Files: 38 source files across 7 directories

## src/extract/ (19 files)
complexity.ts: ComplexityScore
conventions.ts: ProjectConventions, formatConventions
depgraph.ts: DepGraph
env.ts: EnvVar, extractEnvVars, formatEnvVars
error-patterns.ts: ErrorPattern
frontend.ts: PageRoute, ComponentInfo, FrontendInfo, extractFrontend, formatFrontend
gotchas.ts: Gotcha, extractGotchas
history.ts: FileHistory, CommitInfo, formatHistory
infra.ts: InfraInfo, InfraService, extractInfra, formatInfra
monorepo.ts: WorkspaceInfo, WorkspacePackage, detectMonorepo
routes.ts: Route, extractRoutes, formatRoutes
scanner.ts: DiscoveredFile, ScanResult, scanFiles
schema.ts: SchemaModel, SchemaField, SchemaRelation, extractSchemas, formatSchemas
scripts.ts: ProjectScripts, extractScripts, formatScripts
security.ts: SecurityWarning, isSensitiveFile, scanForSecrets, redactSecrets
signatures.ts: Symbol, ImportRef, FileExtraction, extractFile
staleness.ts: StalenessReport, checkStaleness, formatStaleness
tests.ts: TestMapping, formatTestContext
usage-examples.ts: UsageExample

## src/deliver/ (5 files)
ci.ts: generateGitHubAction
claudemd.ts: updateClaudeMd, saveSkeletonFile
cross-tool.ts: writeCursorRules, writeAgentsMd
git-hook.ts: installGitHook, removeGitHook
hooks.ts: installHooks, generateHookScripts

## src/commands/ (4 files)
bench.ts: benchCommand
doctor.ts: doctorCommand
init.ts: initCommand
stats.ts: statsCommand

## src/utils/ (3 files)
detect.ts: StackInfo, detectStack, extToLanguage, PARSEABLE_EXTENSIONS, SKIP_DIRS
pagerank.ts: GraphNode, computeRefCounts
tokens.ts: countTokens, formatTokens, formatBytes

## src/bench/ (2 files)
metrics.ts: TaskMetrics, parseResult
runner.ts: BenchTask, BenchResult, RunOptions, runBenchmark, generateReport

## src/generate/ (3 files)
index-file.ts: ModuleEntry, ModuleIndex, writeModuleIndex
skeleton.ts: SkeletonOptions

## src/learn/ (1 files)
tracker.ts: loadStore, saveStore, generateLearningHookScript

<!-- briefed skeleton: 36 files, ~548 tokens -->
Conventions:
  - camelCase for functions and methods
  - PascalCase for types, classes, and interfaces
Error handling:
  - Uses Result/Either types for error propagation (not exceptions)
  - Prefers try/catch wrapping over throwing
  - Uses guard clauses (early returns on validation failure)
  - Uses schema validation (Zod/Joi/Yup) for input validation
Commands:
  build: tsc
  dev: tsc --watch
  lint: tsc --noEmit
  start: node dist/cli.js
Environment variables:
  config: X (required), VAR_NAME (required), VAR (required), APPDATA (required)

Conventions:
  - camelCase for functions and methods
  - PascalCase for types, classes, and interfaces
